# Jenkins integration — copy to .env and fill in your values
# The full URL to trigger a Jenkins job, e.g.:
#   http://jenkins.example.com/job/dsrc-static-deploy/build?token=MY_TOKEN
JENKINS_WEBHOOK_URL=

# Jenkins user for Basic Auth (default: admin)
JENKINS_USER=admin

# Jenkins API token or password
JENKINS_TOKEN=
